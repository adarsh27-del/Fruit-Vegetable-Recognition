services:
  - type: web
    name: fruit-vegetable-recognition
    env: python
    plan: free

    buildCommand: |
      pip install --upgrade pip
      pip install tensorflow==2.19.0
      pip install -r requirements.txt

    startCommand: python app.py
